---
quick_id: 260425-5vu
slug: change-light-theme-hero-section-backgrou
description: Change light theme hero section background from dark navy to cream/off-white
date: 2026-04-25
must_haves:
  - In light mode (:root), HeroBand renders a cream/off-white gradient background (not dark navy)
  - In dark mode ([data-theme="dark"]), HeroBand still renders the original dark navy gradient
  - All text and UI elements in HeroBand remain legible in both themes
  - No hardcoded dark-only colors remain in the light-theme hero path
---

# Quick Task 260425-5vu: Light Theme Hero Cream Background

## Objective
The hero section (`HeroBand.tsx`) is always dark regardless of theme. Update it so the light theme renders a warm cream/off-white background with appropriately adapted text and panel colors.

## Tasks

### Task 1: Update globals.css hero color tokens

**File:** `src/app/globals.css`

**Action:**
1. In `:root`, replace the existing hero variables (currently set for dark) with light-theme values:
   - `--hero-bg-gradient`: `radial-gradient(ellipse at 65% 40%, #ede5d5 0%, #f5f0e8 65%)`
   - `--hero-ink`: `#0b0f19`
   - `--hero-ink-2`: `rgba(11, 15, 25, 0.6)`
   - `--hero-ink-3`: `rgba(11, 15, 25, 0.35)`
   - `--hero-border`: `rgba(0, 0, 0, 0.1)`
   - `--hero-surface`: `rgba(0, 0, 0, 0.03)`
   - `--hero-button-bg`: `#0b0f19`
   - `--hero-button-ink`: `#f5f0e8`
   - Remove old `--hero-bg` (replaced by `--hero-bg-gradient`)

2. In `[data-theme="dark"]`, add the dark hero variables:
   - `--hero-bg-gradient`: `radial-gradient(ellipse at 65% 40%, #1a2035 0%, #0b0f19 65%)`
   - `--hero-ink`: `#f1faee`
   - `--hero-ink-2`: `rgba(241, 250, 238, 0.6)`
   - `--hero-ink-3`: `rgba(241, 250, 238, 0.35)`
   - `--hero-border`: `rgba(255, 255, 255, 0.1)`
   - `--hero-surface`: `rgba(255, 255, 255, 0.05)`
   - `--hero-button-bg`: `#f1faee`
   - `--hero-button-ink`: `#0b0f19`

**Verify:** grep shows `--hero-bg-gradient` in both `:root` and `[data-theme="dark"]`

### Task 2: Update HeroBand.tsx to consume CSS variables

**File:** `src/components/home/HeroBand.tsx`

**Action:**
1. Replace JS const color values with CSS variable references:
   - `ink2`: `"var(--hero-ink-2)"`
   - `ink3`: `"var(--hero-ink-3)"`
   - `heroBorder`: `"var(--hero-border)"`
   - `heroSurface`: `"var(--hero-surface)"`

2. Update section background to use CSS variable:
   - From: `style={{ background: "radial-gradient(ellipse at 65% 40%, #1a2035 0%, #0b0f19 65%)" }}`
   - To: `style={{ background: "var(--hero-bg-gradient)" }}`

3. Update hardcoded `"#f1faee"` ink colors to `"var(--hero-ink)"`:
   - h1 `color: "#f1faee"` → `color: "var(--hero-ink)"`
   - Stats numbers `color: "#f1faee"` → `color: "var(--hero-ink)"`
   - Panel header status text `color: "rgba(241,250,238,0.7)"` → `color: "var(--hero-ink-2)"`

4. Update form input inline styles to use CSS variables:
   - `background: "rgba(255,255,255,0.08)"` → `background: "var(--hero-surface)"`
   - `border: "1px solid rgba(255,255,255,0.12)"` → `border: "1px solid var(--hero-border)"`
   - `color: "#f1faee"` → `color: "var(--hero-ink)"`

5. Update "Find roof" button inline styles:
   - `background: "#f1faee"` → `background: "var(--hero-button-bg)"`
   - `color: "#0b0f19"` → `color: "var(--hero-button-ink)"`

6. Update "Book an inspection" button:
   - `border: '1px solid rgba(241,250,238,0.25)'` → `border: \`1px solid var(--hero-border)\``
   - `color: "rgba(241,250,238,0.75)"` → `color: "var(--hero-ink-2)"`

7. Leave hardcoded dark colors on SVG overlay badges (address + "AI · Recoloured" badges) — these overlay a satellite image and should remain dark.

**Verify:** No `rgba(241,250,238` or `#f1faee` or `#0b0f19` in hero-path inline styles (except SVG overlay badges)
